# optional, the weather falls back to a mock forecast without it
OWM_API_KEY=

# leave unset and a random key gets saved in .secret_key
# SECRET_KEY=

STORAGE_RISK_LEAD_DAYS=5

# optional, voice booking and the help chat use the offline parser / FAQ without it
GEMINI_API_KEY=
# GEMINI_MODEL=gemini-flash-lite-latest,gemini-3.5-flash-lite

# real calls per day, dry runs don't count
# CALLS_PER_DAY=20

# shared secret for /ivr/status.json, the feed is off without it
# IVR_TOKEN=
